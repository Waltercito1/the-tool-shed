<%= conditional_errors(@user) %>

<div class="new-form">
    <header>
        <h2>Signup</h2>
    </header>
    <%= form_with(model: @user) do |f| %>
        <%= render partial: "form", locals: {f: f} %>
           
    <div class="form-button"><%= f.submit %></div>
<% end %>

<div>
    Already have an account? <%= link_to "Log in", login_path, class: "show-login"%> here.
</div>